type TodoList @model {
  id: ID!
  title: String!
  todos: [Todo]
}

type Todo @model {
  id: ID!
  title: String!
  complete: Boolean!
}

type User @model {
  id: ID!
  name: String!
  todoLists: [TodoList]
}

type Item @model {
  id: ID!
  title: String!
}